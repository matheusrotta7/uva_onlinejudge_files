#include <bits/stdc++.h>

using namespace std;

bool comp (int a, int b) {
    return a < b;
}

int main() {
    // priority_queue <pair<int,int>, vector<pair<int,int>>, greater<pair<int,int>>> Q;
    //
    // Q.push(pair<int,int>(34, 0));
    // Q.push(pair<int,int>(-22, 1));
    // Q.push(pair<int,int>(0, 2));
    // cout << Q.top().second << '\n';
    // Q.pop();
    // cout << Q.top().second << '\n';
    // Q.pop();
    // cout << Q.top().second << '\n';
    // Q.pop();
    pair<int,int> p(3, 5);
    cout << p << '\n';
    return 0;
}
